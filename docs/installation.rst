============
Installation
============

At the command line::

    $ easy_install django-mapfiles

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-mapfiles
    $ pip install django-mapfiles